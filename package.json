{
  "name": "reliable-conversation-runtime",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Caygnus Problem 5 - a bounded runtime for one streamed conversational turn",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "bench": "tsx bench/benchmark.ts",
    "cli": "tsx src/cli/main.ts",
    "serve": "tsx src/http/server.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.7"
  }
}
